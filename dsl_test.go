package gosm

import (
	"testing"
)

func TestDSLParse(t *testing.T) {
	//compiler, err := Parse(dslRaw)
	//err := compiler(funcs)
	//goassert.That(t, err).Equal(nil)
	//machine := GetMachine("s1")
	//goassert.That(t, err).NotEqual(nil)
}
